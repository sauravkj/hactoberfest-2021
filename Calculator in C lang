#include <stdio.h>

int main()
{
  int a,b,c;
  printf("Enter the number : ");
  scanf("%d", a);
  
  printf("Enter the number : ");
  scanf("%d", b);
  
  add = a + b;
  sub = a - b;
  
  //continue the rest
}
